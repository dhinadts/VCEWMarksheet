import Link from "next/link";
import { ArrowUpRight, BookOpenCheck, ClipboardCheck, School, UsersRound } from "lucide-react";
export function DashboardView({title,subtitle,stats,links}:{title:string;subtitle:string;stats:{label:string;value:number|string;hint:string;icon:"users"|"school"|"books"|"checks"}[];links:{label:string;href:string;description:string}[]}){const icons={users:UsersRound,school:School,books:BookOpenCheck,checks:ClipboardCheck};return <><div className="page-heading"><div><p className="eyebrow navy">Academic year 2026–2027</p><h1>{title}</h1><p>{subtitle}</p></div></div><section className="stat-grid">{stats.map(s=>{const Icon=icons[s.icon];return <article className="stat-card" key={s.label}><div className="stat-icon"><Icon/></div><span>{s.label}</span><strong>{s.value}</strong><small>{s.hint}</small></article>})}</section><section className="content-card"><div className="section-heading"><div><h2>Quick access</h2><p>Continue with the most common academic tasks.</p></div></div><div className="quick-grid">{links.map(l=><Link href={l.href} key={l.href} className="quick-link"><div><strong>{l.label}</strong><span>{l.description}</span></div><ArrowUpRight/></Link>)}</div></section></>}
